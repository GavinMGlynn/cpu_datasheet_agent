# adapters

Distributor and part-data adapters (Modules 7 to 9). Each adapter owns its auth, request layer, response schemas, and mapping to core types. All calls go through the cache and the ledger.
