# log

Structured logging and the tool-call ledger (Module 2). JSON lines to stderr, never stdout. Every tool call is appended to a daily JSONL ledger with inputs and outputs; the ledger is the eval dataset.
